---
title: "线性代数基础1"
date: 2025-06-09
categories: [数学,英语]
tags: [线性代数, 教程]
format: html
image: ../images/test.png 
---

这是行内公式 $E=mc^2$。



这是块级公式：

$$
\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
$$

```cpp
#include <iostream>
using namespace std;

int main() {
    cout << "Hello, world!" << endl;
    return 0;
}
```


:::: {.columns}

::: {.column width="50%"}
![图](../images/test.png)
:::

::: {.column width="50%"}

我们使用以下公式来计算损失函数：

$$
L = \sum_i (y_i - \hat{y}_i)^2
$$

还可以在下面加入代码：

```python
loss = ((y - y_pred)**2).sum()
```
:::: 


:::: {.columns}

::: {.column width="30%"}
![左图](../images/test.png)
:::

::: {.column width="30%"}
![右图](../images/test.png)
:::

::: {.column width="40%"}
这是右侧的文字区域，用来对两张图片进行说明或对比。
:::

::::



<img src="../images/test.png" alt="描述图" style="float: left; margin: 0 20px 10px 0; width: 300px;">

这是环绕图片的文字。文字会自动围绕左边的图像排布。适用于文字较多、希望图像贴边的排版风格。

你可以继续写好几段，图片都不会被推下去。
这是环绕图片的文字。文字会自动围绕左边的图像排布。适用于文字较多、希望图像贴边的排版风格。

你可以继续写好几段，图片都不会被推下去。

这是环绕图片的文字。文字会自动围绕左边的图像排布。适用于文字较多、希望图像贴边的排版风格。

你可以继续写好几段，图片都不会被推下去。
这是环绕图片的文字。文字会自动围绕左边的图像排布。适用于文字较多、希望图像贴边的排版风格。

你可以继续写好几段，图片都不会被推下去。这是环绕图片的文字。文字会自动围绕左边的图像排布。适用于文字较多、希望图像贴边的排版风格。

你可以继续写好几段，图片都不会被推下去。这是环绕图片的文字。文字会自动围绕左边的图像排布。适用于文字较多、希望图像贴边的排版风格。

你可以继续写好几段，图片都不会被推下去。这是环绕图片的文字。文字会自动围绕左边的图像排布。适用于文字较多、希望图像贴边的排版风格。

你可以继续写好几段，图片都不会被推下去。这是环绕图片的文字。文字会自动围绕左边的图像排布。适用于文字较多、希望图像贴边的排版风格。

:::: {.columns}

::: {.column width="60%"}
文字内容，前文引导
:::

::: {.column width="40%"}
![示意图](../images/test.png)
:::

::::



:::: {.columns}

::: {.column width="30%"}
![图1](../images/test.png)
:::

::: {.column width="30%"}
![图2](../images/test.png)
:::

::: {.column width="40%"}
多张图对比说明，适合展示产品细节、数据对比等
:::

::::








